apiVersion: v1
kind: Service
metadata:
  name: docs-nginx-service
spec:
  selector:
    app: docs-nginx
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 80    
  type: ClusterIP       